server:
  port: 10081
  servlet:
    context-path: /rmq-cms

# dubbo配置
dubbo:
  application:
    name: rmq-cms
    logger: slf4j
  registry:
    address: zookeeper://127.0.0.1:2181
  consumer:
    check: false